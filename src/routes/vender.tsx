import { createFileRoute } from "@tanstack/react-router";
import { FormEvent, useState } from "react";
import { MessageCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { waLink } from "@/lib/whatsapp";

export const Route=createFileRoute("/vender")({head:()=>({meta:[{title:"Venda seu Gado | Noro"},{name:"description",content:"Apresente seu lote à Noro e inicie uma conversa direta pelo WhatsApp."},{property:"og:title",content:"Venda seu Gado | Noro"},{property:"og:description",content:"Conte o que você tem disponível e fale diretamente com a Noro."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Vender});
const field="h-12 w-full border-b border-input bg-transparent px-0 text-sm outline-none transition-colors placeholder:text-muted-foreground focus:border-primary";
function Vender(){const [sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const msg=`Olá, Noro! Tenho um lote para apresentar.\nNome: ${f.get("nome")}\nCidade: ${f.get("cidade")}\nTelefone: ${f.get("telefone")}\nCategoria: ${f.get("categoria")}\nRaça: ${f.get("raca")}\nQuantidade: ${f.get("quantidade")}\nPeso aproximado: ${f.get("peso")}\nObservações: ${f.get("mensagem")}`;setSent(true);window.open(waLink(msg),"_blank","noopener,noreferrer")}return <main className="min-h-screen bg-hero px-5 pb-24 pt-36 text-hero-foreground md:px-10"><div className="mx-auto grid max-w-[1440px] gap-14 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-gold">Venda seu gado</p><h1 className="mt-5 font-display text-6xl leading-none md:text-8xl">Conte o que você tem disponível.</h1><p className="mt-7 max-w-md leading-7 text-hero-foreground/65">Envie as informações iniciais. A conversa continua diretamente pelo WhatsApp, sem cadastro ou burocracia.</p></div><form onSubmit={submit} className="bg-background p-6 text-foreground md:p-10"><div className="grid gap-x-8 gap-y-5 md:grid-cols-2"><label className="text-xs font-bold uppercase tracking-[.12em]">Nome<input required name="nome" className={field} placeholder="Seu nome"/></label><label className="text-xs font-bold uppercase tracking-[.12em]">Cidade<input required name="cidade" className={field} placeholder="Cidade / PR"/></label><label className="text-xs font-bold uppercase tracking-[.12em]">Telefone<input required name="telefone" type="tel" className={field} placeholder="(00) 00000-0000"/></label><label className="text-xs font-bold uppercase tracking-[.12em]">Categoria<input required name="categoria" className={field} placeholder="Ex.: novilhas"/></label><label className="text-xs font-bold uppercase tracking-[.12em]">Raça<input name="raca" className={field} placeholder="Ex.: Nelore"/></label><label className="text-xs font-bold uppercase tracking-[.12em]">Quantidade<input required name="quantidade" type="number" min="1" className={field} placeholder="Número de cabeças"/></label><label className="text-xs font-bold uppercase tracking-[.12em] md:col-span-2">Peso aproximado<input name="peso" className={field} placeholder="Ex.: 380 kg por cabeça"/></label><label className="text-xs font-bold uppercase tracking-[.12em] md:col-span-2">Observações<textarea name="mensagem" rows={4} className="mt-3 w-full resize-y border border-input bg-transparent p-4 text-sm outline-none focus:border-primary" placeholder="Conte um pouco sobre o lote"/></label></div><Button type="submit" size="xl" className="mt-8 w-full"><MessageCircle/>Continuar no WhatsApp</Button>{sent&&<p className="mt-4 text-center text-sm text-muted-foreground">A conversa foi preparada no WhatsApp.</p>}</form></div></main>}